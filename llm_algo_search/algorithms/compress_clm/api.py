from torch import nn

class API:
    def compress(self, big_model, small_model):
        """
        This method should set the weights of small_model, which is randomly initialized.

        Both inputs are huggingface transformer models
        """
        pass
